feat(admin+frontend): Add car recommendation, popularity, and detail page improvements

- Admin panel car form updated:
  * Added checkboxes for `isRecommended` and `isPopular`.
  * Bound checkbox values properly to Prisma schema fields.
  * Fixed checkbox handling to store correct boolean values in database.

- Prisma schema alignment:
  * Ensured `passengerLimit` is consistently used across backend and frontend (was previously `passengerCapacity`).
  * Updated frontend types and Car interfaces to reflect accurate field names.

- Car detail page enhancements:
  * Migrated page to fully client-side using `useParams()` for dynamic routing.
  * Added loading state and proper fallback when car is not found.
  * Fixed issues with accessing `params` Promise in Next.js.
  * Displayed car details: type, passenger capacity, transmission, fuel capacity, price, and images.

- API improvements:
  * Created robust GET `/api/cars/[id]` endpoint for fetching single car details.
  * Added error handling for invalid IDs, missing cars, and internal server errors.
  * Added console logs for debugging and tracing fetch requests.

- Popular car section improvements:
  * Fetching popular cars based on `isPopular` and `rentalsCount`.
  * Sorted and sliced top 10 cars for display.
  * Fixed mismatch between database field names and frontend interface.

- General improvements:
  * Improved consistency and type safety in frontend TypeScript interfaces.
  * Applied clean and readable code structure for maintainability.
  * Added comments and structured JSX for clarity.
